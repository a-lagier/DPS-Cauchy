out_dir: ./generated_data
device: cuda:0
sampling_size: 256 # if conditional choose it accordingly with dataset image size
model: unet
ckpt_file: ./models/ffhq_10m.pt
steps: 1000
seed: 1
batch_size: 32
channels: 3
conditional:
  use_conditional: True
  dataset: CelebA
  dataset_dir: ./datasets/
  scale: 1.2
  operator:
    name: inpainting
    resolution_factor: 0.5
    mask_type: box # random or box
    mask_size: 10
    mask_density: 0.9
    kernel_size : 60
    intensity: 1.0
  noise:
    name: gaussian
    rate: 1.0
    std: 0.05
metric:
  name: psnr